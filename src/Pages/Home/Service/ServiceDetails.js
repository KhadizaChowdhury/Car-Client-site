import React from 'react';

const ServiceDetails = ({service}) => {
    return (
        <div>
            {
                service.title
            }
        </div>
    );
};

export default ServiceDetails;